#include <iostream>
using namespace std;

int main()
{
	//1
	/*double air;
	int time;
	cout << "Введите растояние ло аэропорта: ";
	cin >> air;
	cout << "Введите время в минутах: ";
	cin >> time;
	cout << air / (time / 60.0) << " км/ч\n";*/
	//2
	/*int sec;
	int work = 8 * 60 * 60;
	cout << "Введите время: ";
	cin >> sec;
	cout << (work - sec) / 3600 << " часов осталось\n";*/
	//3
	/*double laptop;
	int amount;
	double discount;
	cout << "Введите цену ноутбука, количество и скидку: ";
	cin >> laptop >> amount >> discount;
	cout << (laptop * amount) * (100 - discount) / 100 << "\n";*/
	//4
	/*int dz;
	int netdz;
	cout << "Введите количество студентов, сдавших домашние задание, и количество «должников»: ";
	cin >> dz >> netdz;
	int temp = dz + netdz;
	cout <<  netdz * 100 / temp;*/
	//5
	/*double day = 86400;
	double time;
	cout << "Введите время с начала дня в секундах: ";
	cin >> time;
	int hour = (day - time) / 3600;
	int min = (day - time) / 60 - hour * 60;
	int sec = (day - time) - min * 60 - hour * 3600;
	cout << hour << " ч " << min << " м " << sec << " с\n";*/
	//6
	double sum;
	int month;
	double per;
	cout << "Введите сумму в гривнах, срок вклада в месяцах и процентную ставку за год: ";
	cin >> sum >> month >> per;
	cout << ((per / 100.0) / 12 * month) * sum + sum<< " денег в конце года\n";
}
